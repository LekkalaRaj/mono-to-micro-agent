apiVersion: v1
kind: Service
metadata:
  name: marketing-service
  labels:
    app: marketing-service
spec:
  selector:
    app: marketing-service
  ports:
    - protocol: TCP
      port:       80
      targetPort: 8080
  type: ClusterIP