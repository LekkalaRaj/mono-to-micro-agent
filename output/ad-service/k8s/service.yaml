apiVersion: v1
kind: Service
metadata:
  name: ad-service
  labels:
    app: ad-service
spec:
  selector:
    app: ad-service
  ports:
    - protocol: TCP
      port:       80
      targetPort: 8080
  type: ClusterIP